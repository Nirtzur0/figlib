# Readback record

**Verdict:** pass

## Intended claim

The 8-point DFT matrix is a basis gallery: column k, drawn as a mini waveform, is the complex exponential of frequency k sampled at n = 0..7 and generated by the k-th root of unity (same hue); the signal x[n] = cos(2 pi 2n/8) + 0.4 sin(2 pi 3n/8) is the hue-matched weighted sum of exactly the columns its spectrum |X_k| lights up, and F is unitary up to 1/8.

## Cold readback

GLANCE: the DFT: the big bracketed matrix of eight colored sampled-cosine columns reads as "the DFT matrix is a bank of sampled sinusoids (basis vectors)," used to represent the signal on the right.

STUDY: the N=8 DFT matrix F_nk = w^nk is sampled complex sinusoids — column k is the k-th root-of-unity power sequence at frequency k — and any signal decomposes as x = (1/8) sum_k X_k f_k, the spectrum |X_k| showing which few columns carry the example. Reader verified by eye: column 0 constant, frequency rises with k, aliasing after k=4, real parts of columns k and 8-k match, stem hues match column hues, roots equally spaced.

Confusions raised: (1) only Re F_nk plotted, imaginary half on trust; (2) connector direction (synthesis formula vs analysis panel order) guessed; (3) the small arc near the circle center unlabeled (guessed 2pi/8); only w and 1 labeled among the roots.

## Notes

Glance is exactly the claim; the studied read recovered the aliasing pairing k <-> 8-k unprompted — the tip-connecting polylines earn their ink. Accepted: Re-only is admitted on the figure (\mathrm{Re}\,F_{nk} label); full complex checks live in assertions (unitarity, fft match, conjugate symmetry). Connector direction ambiguity accepted — the synthesis equation is the claim and is printed on the connector. The unlabeled arc is the angle 2\pi/8 between the labeled roots 1 and \omega; the two labels bound it.
