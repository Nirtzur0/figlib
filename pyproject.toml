[project]
name = "figlib"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "nirtzur", email = "nirtzur0@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "cairosvg>=2.9.0",
    "numpy>=2.2.6",
    "pytest>=9.1.1",
    "scipy>=1.15.3",
    "ziamath>=0.13",
]

[build-system]
requires = ["uv_build>=0.10.3,<0.11.0"]
build-backend = "uv_build"
